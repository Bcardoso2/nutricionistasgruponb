import 'package:flutter/material.dart';

class EntregasAmostraPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Entregas de Amostra'),
      ),
      body: Center(
        child: Text('Conteúdo das Entregas de Amostra'),
      ),
    );
  }
}
